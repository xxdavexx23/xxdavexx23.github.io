// `app/dashboard/page.tsx` is the UI for the `/dashboard` URL
export default function Page() {
    return <h1 className="flex min-h-screen flex-col items-center justify-between">
        Hello, Dashboard Page!</h1>
}